<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>Document</title>
  <!-- 新 Bootstrap4 核心 CSS 文件 -->
  <link rel="stylesheet" href="https://cdn.bootcss.com/bootstrap/4.1.0/css/bootstrap.min.css">

  <!-- jQuery文件。务必在bootstrap.min.js 之前引入 -->
  <script src="https://cdn.bootcss.com/jquery/3.2.1/jquery.min.js"></script>

  <!-- popper.min.js 用于弹窗、提示、下拉菜单 -->
  <script src="https://cdn.bootcss.com/popper.js/1.12.5/umd/popper.min.js"></script>

  <!-- 最新的 Bootstrap4 核心 JavaScript 文件 -->
  <script src="https://cdn.bootcss.com/bootstrap/4.1.0/js/bootstrap.min.js"></script>
</head>
<style>
  body {
    text-align: center;
    padding: 100px 0;
  }

  form {
    width: 300px;
    margin: 0 auto;
  }
</style>

<body>
  <div class="container">

    <table class="table">
      <thead>
        <tr>
          <th>导航名称</th>
          <th>导航位置</th>
          <th>导航路径</th>
          <th>导航排序(大的靠后显示)</th>
          <th>导航管理</th>
        </tr>
      </thead>
      <tbody>
        <% categories.forEach(item => { %>
          <% var pid = item.pid == 1 ? '顶部菜单' : '左侧菜单' %>
          <tr>
            <td><%= item.name %></td>
            <td><%= pid %></td>
            <td><%= item.path %></td>
            <td><%= item.rank %></td>
            <td><a class="btn btn-sm btn-outline-danger" href="/admin/del?id=<%= item.id %>" onclick="return confirm('你确定删除这个导航吗？删除这个导航所有子文章也都会被删除！！！')">删除</a></td>
          </tr>
        <% }) %>
      </tbody>
    </table>
    <ul class="pagination">
      <li class="page-item"><a class="page-link" href="#">Previous</a></li>
      <li class="page-item"><a class="page-link" href="#">1</a></li>
      <li class="page-item active"><a class="page-link" href="#">2</a></li>
      <li class="page-item"><a class="page-link" href="#">3</a></li>
      <li class="page-item"><a class="page-link" href="#">Next</a></li>
    </ul>

    <form action="" method="POST">

      <div class="form-group">
        <label for="name">导航名称:</label>
        <input type="text" name="name" value="" class="form-control" id="name">
      </div>

      <div class="form-group">
        <label for="pid">导航栏位置:</label>
        <select class="form-control" name="pid" id="pid">
          <option value="2">左侧菜单</option>
          <option value="1">顶部菜单</option>
        </select>
      </div>

      <div class="form-group">
          <label for="path">导航路径:</label>
          <input type="text" name="path" value="" class="form-control" id="path">
        </div>

      <div class="form-group">
        <label for="rank">导航栏排序:</label>
        <input type="number" name="rank" value="" class="form-control" id="rank">
      </div>

      <div class="form-group">
        <label>是否显示导航:</label>
        <div class="form-control">
          <label><input type="radio" name="is_hide" value="1" checked>是</label>
          <label><input type="radio" name="is_hide" value="0">否</label>
        </div>
      </div>

      <div class="form-group">
        <label>是否显示内容:</label>
        <div class="form-control">
          <label><input type="radio" name="is_display" value="1" checked>是</label>
          <label><input type="radio" name="is_display" value="0">否</label>
        </div>
      </div>

      <button type="submit" class="btn btn-primary">Submit</button>
    </form>

  </div>
</body>

</html>